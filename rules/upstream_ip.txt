# 上游服务器真实 IP —— 校园网 DNS 被封锁时的最后兜底
# ------------------------------------------------------------------
# 正常情况保持全部注释即可: 代理会自动解析 (缓存 / 公共UDP DNS / 系统DNS / DoH)
# 仅当 netcheck.py 显示所有 DNS 层都失败时, 才需要在这里手动写一行真实 IP:
#
# 8.134.221.255
#
# 写入后双击 start.bat 生效 (会通过 XLB_UPSTREAM_IP 环境变量传给代理)。
# 查真实 IP 的方法: 在能上网的手机/电脑上 ping xlb.810086.com
